{
  "name": "Latencymap",
  "install": "npm ci",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev:local",
      "description": "Next.js app on http://localhost:3000. Set PROBE_WORKERS_SUBDOMAIN and PROBE_SECRET in .env.local to run real regional tests against deployed Workers."
    }
  ],
  "ports": [
    { "name": "app", "port": 3000 }
  ]
}
